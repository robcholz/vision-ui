[project]
name = "vision-ui-scripts"
version = "0.1.0"
description = "Python environment for repository scripts"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.9",
]

[tool.uv]
package = false
