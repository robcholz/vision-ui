name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang clang-tidy clang-format xmake libsdl2-dev

      - name: Fetch u8g2
        run: |
          mkdir -p components
          if [ ! -d components/u8g2 ]; then
            git clone --depth=1 https://github.com/olikraus/u8g2.git components/u8g2
          fi

      - name: Configure project
        run: xmake f --simulator=y

      - name: Generate compilation database
        run: xmake project -k compile_commands

      - name: Clang-Format check
        run: |
          set -e
          files=$(git ls-files '*.c' '*.cc' '*.cpp' '*.cxx' '*.h' '*.hpp' '*.hh' '*.hxx' | grep -v '^include/font/')
          if [ -n "$files" ]; then
            clang-format --style=file --dry-run --Werror $files
          fi

      - name: Clang-Tidy check
        run: |
          find . -type f \( -name '*.c' -o -name '*.cpp' \) \
          ! -path '*/components/*' \
          | xargs clang-tidy -p . --warnings-as-errors='*'

      - name: Build
        run: xmake
